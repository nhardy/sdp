@import '~app/assets/stylus'


html
body
:global(#root)
  height: 100%

body
  background: $backgroundColor
  color: $textColor

a:hover
  text-decoration: none

:global(#root)
  font-family: SourceSansPro

strong
  font-weight: bold
