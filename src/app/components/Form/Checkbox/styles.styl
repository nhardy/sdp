@import '~app/assets/stylus'


$width = 44px
$height = $width / 2
$scale = 1.5

.root
  display: inline-block
  width: $width * $scale
  height: $height * $scale

.switch
  transform-origin: 0 0 0
  transform: scale($scale)
